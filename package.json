{
  "name": "social-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Production-grade Instagram-inspired social media web application.",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --parallel --filter @social/api --filter @social/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}
